# History — why the rules exist

The why-log: one line per change, newest first. Instruction files do not depend on it; every
rule lives in its own file. Entries older than ~3 weeks are pruned; the full history is in git
(`git log -p -- references/history.md`).

## Changes

- **2026-09-23 — retro (v0.3.6), cleanup: `merge-pr`, `close-issue` and `close-epic` run one `cleanup_unit` (review + dev worktree, landed branch deleted on origin and locally, unmerged work kept, `worktree prune`); gh `--delete-branch` dropped; `close-epic` also sweeps children, archives run-state and tears the stack down; new `review-worktree-add`/`release-review-worktree`/`prune-stale`; agent scratch lives in `<worktree>/.sdlc-scratch/`.** Weeks of runs leaked 200+ branches, orphan worktrees and ~970 `/tmp/sdlc-*` files: `--delete-branch` failed silently on a worktree-checked-out branch, local refs were never deleted, review trees were prose-only.
- **2026-09-23 — retro (v0.3.6), fixes: backtick-wrapped Task field lines parse, an unparsed `Depends on` refuses its section (`finish-lld` stops); `open-gate` derives and normalises its title; a docs-only reconcile keeps exploratory evidence; `create-issue`/`repair-issue` name the 100-sub-issue cap (`reason_code: sub_issue_cap`); `terminal_count` is run-wide; one glob helper, re-attest honours `excludeGlobs`. Prose: instructions no longer restate what a command or hook does (one pointer each), composites replace building-block sequences, operator summaries at pickup / clean arch-review / clean product-review, regate resume needs no `set-stage`, IRD facts stated once, auto-mode denial note.** Epics #561/#1027/#1028/#94, Initiatives #560/#810; operator: a script's deterministic work must not also be an agent instruction.
- **2026-09-23 — the gate workflow is one job: `open-gate` labels each gate PR `sdlc:gate` and `auto-advance` runs only for labelled PRs; `merge-pr` sets Stage/Pipeline Status itself, so the `issues: closed` job (`mark-issue-closed`) and its `init-todo-status` sibling are gone from the shipped workflow (v0.3.5).** Operator: the workflow woke a runner for every issue close and every `issue-*` PR close, then skipped most in the script. Closed-issue fields are read by nothing (readers filter to open); a human close now leaves them stale.
- **2026-09-22 — retro (v0.3.4), LLD→Task: `create-lld-tasks` carves only `## Task` sections carrying a `## Footprint` (`skipped_sections`); `Realises: #n` blocks the named pre-existing issues on the Task, `merge-lld-doc` advances only carved, unrealised children (`not_advanced`), and `merge-pr`/`close-issue` close realised issues (`Closes #` never fires on an `epic-<n>` merge); `find_footprint` tells a missing section (skipped) from an owned-nothing one (schedulable) and only `**Verify-only:**` ends the owned list; `lld.md` holds design only — no attestation/CI/run-count/credential mechanics (`lld-review` bounces them), carving summaries never under `## Task`.** #813: a `## Task …: summary` table became issue #879 and bug #628 entered the pool footprint-less; #816: four pre-filed children were delegated beside the Tasks realising them, #984/#985 skipped as "no ## Footprint", and the LLD's gate procedures bounced #984 on criteria the pipeline never required.
- **2026-09-22 — retro (v0.3.4), evidence: a required suite on a PR is satisfied by a passing GHA check for its workflow on the head OR a `record-local-ci` attestation (`pr_stale_attestations` reads check runs via `workflow_check_state`, shared with the merge gate; a held suite reports `check` + `reason`); `attestable: false` suites are never held at review start and surface as `awaiting_checks` at epic close; `development` stops at the first clean suite pass, relies on the PR's checks and attests only config-required suites; close-epic evidence carries forward — the exploratory record across a delta inside `pipeline.epicClose.evidenceCarryForward.paths` (docs by default), a suite attestation across a delta outside its coverage, and the merged children's attestations/checks chain to the epic head (`evidence`, `carried_forward_files`, `evidence_breaks`; `--no-carry-forward` restores head-only strictness).** #984's pr-review bounced an empty-diff PR for local attestations while `Backend Validate`/`Frontend Validate` had run green on every PR (~10 min redundant); #985's dev spent ~55 of 88 min on unrequested diagnostic runs; every closing-delta doc commit forced a full exploratory + suite re-run.
- **2026-09-22 — retro (v0.3.4), control plane: `next-action <epic>` on a bare Epic returns `cut-phase-tasks` instead of `none`; `audit-issues` lists children before the container (`container: true`); `open-dev-pr --allow-empty` for a verify-only Task; `detach-epic`, `close-issue --not-planned --reason`, `comment`; `close-issue` deletes a merged design PR's branch; `pipeline.worktrees.releaseCommand` runs before every worktree removal; the guard's `sync-branch` denial tells a stage agent to stop `blocked` naming `sync-branch <n>`; `guard.mainThreadFreshnessHours` (default 8) and one stderr line when the main thread runs unguarded; one dev-lane slot definition (`dev_lane_slots`: started and not parked/blocked) shared by `next-action` and `list-parallel-ready` (`slots`, `dev_lane.deferred`), other epics' worktrees hold no slot (`parallelism.crossEpicFootprintCheck`); review fan-out `maxChildren` is a concurrent cap with `retry_after {waiting_on, hint}` and an unreadable `ROLE:` header falls back to the stricter policy; citation resolver fails closed; `merge-pr` squashes with `--match-head-commit`; the plugin repo runs its own pytest suites in CI; operations.md documents the plugin pin and the `Edit(//tmp/sdlc-review-*/**)` allow rule.** #816's orchestrator hand-spotted the missing phase-Tasks; #741 closed COMPLETED with no comment; 10 orphan `it-pg-*` envs leaked; a dev agent hand-merged `origin/epic-N`; `next-action` handed out units the pool refused; #466/#182/#524 backlog.
- **2026-09-20 — retro (v0.3.3): development authors no design doc (`open-dev-pr`/`verify-exit` refuse a diff touching `epic-*/**` or another Task's Footprint; agent text no longer tells dev to amend `lld.md`); `start-comment pr-review` refuses a stale post-sync local-CI attestation; `record-local-ci` refuses `<placeholder>` commands; `file-closing-delta` takes Critical/Blocker as Urgent and self-repairs; `next-action resume` carries `likely_live`; `merge-pr --run-id`; `start-stage` ensures the epic branch first; `requiredWorkflows[].bases` and suite `attestable: false`; `lld` opus and `lld-review` maxChildren 4 (total, not concurrent); `lld` searches via Explore/Grep with one proof sweep.** #690/#691 bounced four times on dev amending `lld.md`; #691 went red on the merged head after a green attestation; a #667 dev-lane `resume` collided with a live session; #673's `lld` spent 124 Bash calls exploring inline.
- **2026-09-20 — v0.3.2 (operator, ahead of the full retro): `close-epic` no longer requires the full-e2e closing verification (the e2e-test Task owns it; `--kind e2e` still recorded, informational) and never gates on GitHub Actions results (only a passing check counts as evidence; failed/pending/never-started ones are ignored; `record-local-ci` attestations remain required).** Epic #671's close was blocked by a missing e2e record and by Actions never starting (spending limit), for evidence already held elsewhere.
- **2026-09-19 — consistency pass (v0.3.1): `record-initiative-verification --outcome met|unmet` and `close-initiative` refuses while the latest record is `unmet`; the Agent guard sets stage models when a continuous-mode cycle agent (not `sdlc:*`) launches a stage; the Bash guard lets `product`/`architecture` run `resolve-thread` on a gate-feedback round; gates docs no longer claim the workflow sets `Feedback Received` (that job was dropped 2026-09-04); `pr-review` always gets a detached review worktree; `lld` must carve the two standing Tasks and `lld-review` checks for them; SubagentStart passes `docTemplates`, which `product`/`architecture` and README now cite; pre-launch is a driven-repo fact (its `CLAUDE.md`), not a plugin one.** A full read of the plugin against itself: a "not met" record could close an Initiative; a nested orchestrator skipped model policy; gates.md told an agent to run a command the guard denied and described a workflow job that no longer exists; a single-lane `pr-review` mutation-probed inside the development worktree.
- **2026-09-19 — retro (d): the Bash guard denies the main thread's hand-run mutations only while its session drives a run (a run-state file for its session, written within 8 h); `sdlc:<role>` agents stay guarded either way; `guard.mainThread: "always"` restores the old rule.** Operator: backlog and issue cleanup outside the pipeline was blocked in every session of the repo. State files are written only by `next-action --run-id` and never deleted, hence the freshness window; an unknown session id stays guarded.
- **2026-09-19 — retro (c): `next-action <initiative>` walks the Initiative's open Epics (`run-epic`, lowest number first, order from native `blockedBy` edges set at cut time by `create-issue --blocked-by`; `cut-phase-tasks` first when an Epic has none; `--skip-epic` parks a stalled one; one run id shares `maxTasksPerRun` across every Epic) and its `none` names each Epic's state; `audit-issues [--initiative]` flags Epics with no phase-Tasks; `merge-gate --operator-confirmed` merges a gate/design PR only on the operator's explicit say-so; gate feedback ends with `SDLC-RESULT done` and the orchestrator runs `mark-feedback-addressed` after `transition` (the agent no longer does); the design-review marker carries the reviewed head SHA and `merge-design-pr` refuses `review_stale`; a design PR a human merged before its gate opened is completed by `pass-gate`/`finish-lld` (`next-action` returns them).** Operator: an Initiative run should drive its Epics itself instead of stopping at "N Epics still open"; a gate PR the operator asks for should not need a hand merge; the gate-feedback doc contradicted the guard (parked in the 2026-09-19 retro backlog).
- **2026-09-19 — retro (b): a non-standing Epic's branch `epic-<n>` and worktree exist from the start and every child (phase-Tasks included) branches from and merges into it; `architecture`/`lld` author `docs/sdlc/epic-<n>/<doc>` directly (`publish-doc` is gone); `transition` raises a marked design PR `issue-<n>` → `epic-<n>` (`open-design-pr`), `record-design-review` comments on it, and the pipeline merges it (`merge-design-pr`: `lld-review` always on clean, `arch-review` above the skip threshold or where Gate B is waived, else the human at Gate B); `finish-lld` merges before creating Tasks; `merge-pr` refuses a phase-Task; `pass-gate` reconciles with the epic base; `pause-for-epic-regate` needs no gate PR; `post-comment` reads Stage/status from `issue_fields`.** Operator: the design PR should be where the design is reviewed and merged, and no Task should ever start from `main` inside an Epic. `post-comment` always saw Stage None / status None because `issue_view` carries no field values.
- **2026-09-19 — retro (v0.2.1): `tokenEnv` and a configured token now override an ambient `GITHUB_TOKEN`; `product`/`architecture`/`lld` hand off via `post-comment` (role-tagged, claim-checked) and the stop hook holds a `done` finish until it ran; `record-epic-verification` stamps the tested head and `close-epic` refuses evidence older than later code, and lists unattested suites; `open-gate` titles a phase-Task PR with its parent's title; `file-closing-delta` files a closing-run Bug; `audit-issues --epic` covers only that subtree; the metrics store resolves the same directory from a Bash call as from a hook; long suites run in the background behind a log sentinel; epic-close preflights the Docker VM; the e2e-test Task pins its retry procedure; e2e fixtures are torn down.** Parked from epics #558/#559/#623: a stray fine-grained `GITHUB_TOKEN` shadowed the classic PAT; agents' handoffs never reached the issue; a stale e2e record passed after later code merged; two e2e runs on an exhausted VM were unrecordable; every PR of a phase-Task read alike; `report --run` read an empty store.
- **2026-09-18 — standing epics hold technical (RTB) tasks and route each child stage by stage: `next-action` returns `route` for a fresh child, `route <n> --to <stage>|merge` skips ahead (forward only; a `stage-route` marker is `merge-pr`'s evidence for a skipped `pr-review`, refused after a rework bounce), stage agents may recommend `"next"`/`"why"`; the Bug fast-track is gone; the standing profile waives both human gates (`requiresHumanGateB`, `waive-gate` replaces `auto-pass-gate-a`); `sdlc_metrics report --by route`.** Operator: not every RTB task needs every stage — decide from each stage's feedback, not an upfront plan; the pr-review skip is the orchestrator's recorded decision, not a path rule.
- **2026-09-18 — retro 2026-09-17 ported: operator-only retros (`retro-check` gone); idempotent `merge-pr`; reconcile fast-forwards first and retries a transient fetch/push once; stale-branch-safe `worktree-add`; no `development` re-claim over an open PR; stricter footprint and `requiredWorkflows` parsing; teardown checks containers stopped; the orchestrator merges and records closing verification; `development`/`lld` may use Explore; metrics add tool calls, peak context, `--run`.** Parked findings from four epics; metrics stay hook-recorded, never orchestrator-fed.
- **2026-09-18 — models and fan-out are hook-enforced from one policy file (`hooks/model_policy.json`); the orchestrator moves to sonnet (`sdlc-run <n>` launches it); only arch/lld-review fan out (≤ 2 sonnet children); stage agents are limited to their role's control-plane commands; comment caps are refused at the source; per-agent cost metrics.** 2,262 transcripts: sonnet children's findings were used as often as opus children's, completeness axis included (80% vs 78%); fanning out cost 2.4–3x a single pass with no higher rework catch for pr-review/product-review; the orchestrator was 15% of cost on opus and is now mostly mechanical — compare metrics after the first epic.
- **2026-09-18 — `repair-issue` fills an existing issue's missing fields (never overwrites) and is what `create-issue` failures and `audit-issues` hints point at; audit no longer flags parentless Epics; a `finish-lld` re-run keeps the numbered `lld.md`; `start-stage --role testing` claims `development`; hooks find config like the script and export an absolute `tokenPath`; the guard parses `sudo`/`xargs`/`timeout`/`env` option values.** "Repair in place" had no command; engineering Epics are parentless by design; a re-publish reverted Task numbering; worktree cwds broke config discovery and relative token paths; `sudo -u x gh …` slipped through.
- **2026-09-18 — review fixes: `create-issue` links the parent first; parked/advanced units get Pipeline Status `Todo`, never a cleared value; the Bash guard also catches implied-POST `gh api`, `gh issue/pr new`, quoted `$(...)`/backticks, shell keywords, `timeout`, `sh -c` and `eval`.** A half-made issue without a parent was duplicated on re-run; `audit-issues` flagged every cleared-status unit; each guard gap let a denied mutation through.
- **2026-09-18 — `create-issue` makes the native Issue Type mandatory and sets parent, Pipeline Status `Todo`, Priority and Effort at creation; `audit-issues` lists gaps; `resolve-thread` replaces the guard's GraphQL exception.** Agent-filed issues (raw `gh issue create`) had no type or parent, bugs were typed `Task`, and Priority/Effort were never set.
- **2026-09-18 — shipped as the `sdlc` Claude Code plugin (`/sdlc:run`, `sdlc:<role>` agents) with hooks: SessionStart env, a Bash guard for control-plane-owned mutations, a SubagentStop check for the `SDLC-RESULT` line. One plugin version per driven repo (submodule, `$SDLC_DIR`, `sync-skill` removed); parallelism defaults to 1.** Priorities: code quality > cost and speed per task > parallelism; guard and result line are enforced by hooks instead of prose.
- **2026-09-18 — docs slimmed ~50%, history pruned to 3 weeks, composite control-plane commands (`cut-phase-tasks`, `finish-lld`, `open-arch-revision`, `start-stage`, `transition`) and phase-Task base auto-detect added.** Cuts per-stage context load and orchestrator round trips.
- **2026-09-16 — V1 lifecycle removed; Initiative → Epic → phase-Tasks is the only flow.** Agent definitions no longer worked with V1; standing/legacy profiles kept.
- **2026-09-16 — a Stage-less child under an architected Epic reports `unstaged` instead of defaulting to `lld`.** The epic-level `lld` agent does no task-level pass.
- **2026-09-16 — architecture deviation re-gates through an Architecture revision phase-Task, not a second Gate B on the epic.** Reuses the phase-Task mechanics; `pause-for-epic-regate` still parks the reporter.
- **2026-09-16 — `sync-branch` got `--base`; a missing base returns `base_missing` instead of crashing.** Phase-Task syncs resolved to the epic branch, which may not exist yet (now auto-detected, 2026-09-18).
- **2026-09-16 — `worktree-add` fast-forwards a resumed branch to origin and refuses a diverged one.** A long-lived epic worktree silently missed merged PRs.
- **2026-09-16 — `verify-exit` reports `handoff_marker_present` on a `pr-review` exit.** A skipped `handoff-to-pr-review` was invisible until `merge-pr` refused stages later.
- **2026-09-16 — `maxTasksPerRun` enforced server-side via `--run-id`; `next-action` returns `stop-at-cap`.** The orchestrator was counting by hand and long runs had no mechanical stop.
- **2026-09-16 — attestation carry-forward requires every base-delta file to be a doc path.** "Doesn't touch a required suite" was vacuously true with a narrow `requiredWorkflows`.
- **2026-09-16 — Task issues created by `create-lld-tasks` after a clean `lld-review`, not by `lld` mid-turn.** A bounced round left half-created sibling issues.
- **2026-09-16 — confidence marker defined; clean zero-blocker verdicts sit at/above threshold; threshold 95 → 80.** A clean `arch-review` self-scored 60, making Gate B's skip unreachable.
- **2026-09-16 — fan-out child replies capped at 1,200 characters.** 22 children returned 8–14K chars each into the parent.
- **2026-09-16 — fan-out only above a size threshold, max 3 children, never on rework, always `model: "sonnet"`.** Reviews were 63% of a ~$194 run; opus-inherited children cost $50 vs $12.5 at sonnet.
- **2026-09-14 — `product-review`/`arch-review` back to opus (Fable pricing).** Fable is $10/$50 vs opus $5/$25 per MTok — 2x, not cheaper; the 2026-09-12 move repeated the 2026-08-28 mistake.
- **2026-09-14 — local `test:it` scoped to touched domains; shared/migration touches run the full suite.** Every child ran all 17 suites.
- **2026-09-14 — a mechanism-behaviour claim in `lld.md` needs a positive+negative throwaway control.** Every blocking finding on #530/#513 was a reasoned, disproved runtime claim.
- **2026-09-14 — a recurring defect class must state why the earlier round missed it, and escalates regardless of strike count.** #521 bounced 4x on one class while the counter never fired.
- **2026-09-14 — `retro-check --mark-done` takes `--count` from the triggering check.** Stamping the live count marked unswept issues as reviewed.
- **2026-09-14 — `sync-skill` added.** Bumping and re-vendoring the skill was the one manual step with no command.
- **2026-09-14 — duplication pass (development gates, fan-out rules, marker timestamp helper).** Same rule in three places drifts.
- **2026-09-14 — `product.md` says each fact once; `lld.md`/`architecture.md` never carry their own review history.** #365 stated facts 4x; #494's `lld.md` hit 1,771 lines of round ledgers.
- **2026-09-14 — `stage-playbooks.md` split into core + `design-doc-rules.md`, `verification-rules.md`, `review-fanout.md`.** Every role read all 594 lines.
- **2026-09-14 — `--same-class-recurrence` flag on review records, counted by `pairing-counts`.** "Escalate on the pattern" in verdict prose was never re-read (#504 4 rounds).
- **2026-09-14 — a finding naming a sibling is posted on that sibling's issue.** #499's note never reached #504.
- **2026-09-13 — one rule, one home, chosen by who needs it; orchestrator decisions moved to `rework.md`.** The no-park rule had four drifting copies; the stalled agent read the weakest.
- **2026-09-13 — review-sourced agent-process feedback loop: STAGED, not wired.** Needs a CLI marker that does not exist; agents would only flag, only the retro edits agent files.
- **2026-09-13 — "Attribution is falsifiable" pre-handoff pass.** #159 lost four rounds to quotes whose real source was the delegation prompt.
- **2026-09-13 — only/every/none claims need the search as a command plus a positive control.** Detectors blind to `require()`/`it.each` variants "proved" false negatives.
- **2026-09-13 — the no-park rule constrains the final message: finished, blocked, or stopped-for-decision.** Restating it harder had stopped working.
- **2026-09-13 — `release_worktree` uses `--force`.** Submodule-bearing worktrees made every remove fail, starving the lane.
- **2026-09-13 — a footprint includes the unit's own spec and test doubles.** Two children both green alone, red on merge over one shared helper.
- **2026-09-13 — Step 5 requires `scripts/tests/test_sdlc_next.py` green plus a regression test.** Two sessions shipped untested control-plane changes.
- **2026-09-13 — derived thresholds quote their criterion; shared boundaries are read, not assumed.** #494 narrowed an AC ~10x and leaked message bodies into logs.
- **2026-09-13 — task-local decision conformance sweep and discriminating assertions (one positive control per family).** #494 shipped an undeclared deviation and vacuous `type === 'text'` tests.
- **2026-09-13 — copy the CI workflow's own invocation before a local run; comments stating a guarantee must hold for every input.** A missing `--runInBand` produced 247 false failures.
- **2026-09-13 — `list-parallel-ready` computes occupied/stale once; `verify-exit` accepts `--expect-stage pr-review`.** Collisions and slots came from different sets; the busiest handoff was uncheckable.
- **2026-09-12 — concurrent multi-epic isolation: ephemeral per-branch worktrees, per-branch `flock`, per-unit skill copy, optional per-epic stacks.** Branch ops stole branches into the main checkout and shared one Docker stack.
- **2026-09-12 — `merge-lld-doc` decides against origin and verifies the blob after push.** It reported `up-to-date` while the doc never reached origin.
- **2026-09-12 — pre-product scope alignment with the operator.** One-line epic issues let scope divergence cascade through every later stage.
- **2026-09-12 — `pr-review` suite re-run keyed to the shape of the evidence.** It re-ran 25+ minutes of Docker for near-zero signal.
- **2026-09-12 — delete stale `*.tsbuildinfo` or assert the artifact before a build counts.** `nest build` emitted nothing and exited 0, twice.
- **2026-09-12 — `opus` alias means latest; never pinned to a version.** A pinned alias runs on a stale model.
- **2026-09-12 — `merge-lld-doc` advances to `development` without claiming.** Lets one `next-action` pass mix lanes and removes an ambiguous crash window.
- **2026-09-12 — comment caps: 2,000 chars per handoff, 6,000 per evidence comment.** Review comments ran 15–24K chars against a "keep it short" rule.
- **2026-09-12 — `pipeline.productWip.maxGateAPending` (default 5).** Standing backlogs could open more Gate A PRs than a human can review.
- **2026-09-12 — cross-epic coordination file DEFERRED.** Build only if real multi-epic contention appears.
- **2026-09-12 — `product-review`/`arch-review` moved to fable.** Reverted 2026-09-14 on cost.
- **2026-09-12 — per-issue doc set closed at `product.md`, `architecture.md`, `lld.md`; development's record is the PR description.** 123 `development.md` and 45 unspecified `testing.md` files nobody read.
- **2026-09-12 — `testing` stage removed; `record-local-ci` requires `--command`/`--output`.** A second agent re-running the suite was duplicative; machine-produced, sha-pinned evidence replaces it.
- **2026-09-12 — `pr-review` reviews against the design and ACs, with a code-health bar and change-detector check.** It was measuring the diff against the implementer's own account.
- **2026-09-11 — subagents finish in one turn; never park on a background notification.** Three agents idled "standing by" for a wake that never comes.
- **2026-09-11 — one Docker-IT-heavy stage at a time; IT suites backgrounded.** Two concurrent suites passed the 600s watchdog and killed both agents.
- **2026-09-11 — truncating suites only against a `_test` DB.** A copied `DB_NAME` override wiped the shared dev DB.
- **2026-09-11 — sweeps include `test/**`; ports return field-by-field projections; worktree drift guard.** `src/**`-only sweeps bounced three children; a raw entity nearly over-disclosed (#473).
- **2026-09-10 — `pipeline.epicClose.auto` lets the orchestrator make the second `close-epic` call.** `close-epic` already refuses on every unsafe state.
- **2026-09-10 — `designLane` (default 2) and `list-design-ready`.** Standing-epic children's product/architecture ran serially.
- **2026-09-09 — never delete a per-issue branch while its issue is open; commit the driven repo's config and submodule pin.** Docs were lost on dangling commits; an uncommitted config flipped Gate A mid-run.
- **2026-09-08 — epic profiles, universal `product-review`, `gates.requiresHumanGateA`.** Move hardcoded policy into client config; `product.md` had no adversarial pass.
- **2026-09-08 — a completeness claim over a footprint is a mechanical sweep, on the authoring side too.** Epic #345 children bounced twice each on hand-listed inventories.
- **2026-09-06 — `merge-pr` closes a child merged into a non-default branch.** `Closes #n` fires only on the default branch.
- **2026-09-06 — `SKILL.md` slimmed; retro watermark lives in the driven repo; personal paths removed.** The watermark path pointed at the skill's old name and fired every run.
- **2026-09-05/06 — epic gate docs routed via `epic-<n>-gate-<stage>` sub-branches.** Superseded by phase-Tasks, 2026-09-16.
- **2026-09-04 — `merge-lld-doc` publishes `lld.md` to the epic branch on a clean `lld-review`; resumes base on `origin/issue-<n>`.** A resume recreated the worktree off `main` and discarded pushed work.
- **2026-09-03 — check the model table against `origin/main` before the first dispatch.** A stale `SKILL.md` sent six dispatches a tier too high (~1.55M tokens).
- **2026-09-03 — `mark-blocked` resets Pipeline Status to `todo`.** A blocked unit looked like a crashed run and was re-offered as `resume`.
- **2026-09-03 — stopping a fan-out parent does not stop its children; salvage their work and sweep probe artifacts.** An axis returned ~166k tokens of work after its parent was killed.
- **2026-09-01 — `lld` back to sonnet; `lld-review` stays opus.** Opus `lld` did not change the failure class.
- **2026-08-28 — epic-level `architecture`/`arch-review` reverted from Fable to opus.** Fable is 2x opus per MTok and its thinking cannot be disabled.
- **2026-08-28 — `record-design-review` and per-role `design_review` counts.** The valve could not see `lld-review ↔ lld`, its busiest pairing.
- **2026-08-28 — development gate 4: check every AC against the real diff.** #284's validators were unit-tested and wired into no entrypoint.
- **2026-08-28 — design reviews state axes launched vs returned.** #274 posted CLEAN with zero of three axes back.
- **2026-08-28 — shipping docs prefer the grep-anchored quote over line numbers.** #274's merge broke four of its own citations.

## Incidents behind standing rules

- **Never hand-type fetch/checkout/merge/push.** A silently failed checkout merged and pushed onto the wrong branch.
- **Branch and diff from `origin/main`, never local `main`.** Local `main` goes stale; epic #156 worktrees started 4 commits behind.
- **Classic PAT only.** The org's Free plan blocks fine-grained PATs on check-runs.
- **Parked/closed units release their worktree; stale ones don't count as slots.** #186's leftover worktree ran the lane at one-third capacity with no error.
- **`product.md` follows the repo's IRD form and names no vendor.** Epic #98's product doc was rejected on ten counts, all written-down pipeline rules.
- **Never a two-dot diff range.** It reported 4 files where the agent had touched 1.
- **Resolve docs by branch name, not a SHA from an old comment.** `pr-review` blocked on an unreachable superseded draft (epic #110).
- **Compile-checking is not verification.** #209's E2E locator passed `--list` and was broken.
- **Establish a number by running the tool, not a grep that models it.** Epic #156 shipped 18+ wrong measurements; a stale `tsbuildinfo` made `tsc` emit 92 of 533 files.
- **A finding this unit won't fix goes to the owning sibling or backlog epic, never only a closing PR.** #243's coverage gap survived only by hand.
- **A replacement agent starts from the committed work.** #238/#251 replacements had to be told by hand.
- **Rework rounds are scoped.** A scoped pass took 277s vs 913s and still found a blocker.
- **Name the recurring class, not the bounce.** #234 and #243 settled one round after the class was named.
- **Commit and push after every step.** #274's batching replacement lost three rounds; per-finding commits survived nine interruptions.
- **Force-push only with `--force-with-lease` after proving a content superset.** A resumed agent hit a push rejected by an intervening rebase.
- **The machine's resource cap is the real cap.** The full IT suite was OOM-killed in the Docker VM.
- **Hold long waits in the orchestrator; run long jobs detached, writing under the bind mount.** Poll-and-re-park burned ~190k tokens; `--rm` containers lost reports.
- **Reviews stay empirical, not diff-only.** Re-running caught an IPv6-mapped-address bypass.
- **Never create the epic branch as an orphan.** `epic-110` had no merge base and could never merge.
